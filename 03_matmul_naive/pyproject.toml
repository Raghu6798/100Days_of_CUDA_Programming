[build-system]
requires = ["setuptools", "torch"]
build-backend = "setuptools.build_meta"

[project]
name = "02_matmul_naive"
version = "0.1.0"
description = "My third kernel function"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
